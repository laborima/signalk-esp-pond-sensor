# SignalK Pi Pond Video Configuration
# Raspberry Pi Zero WH with Camera Module v3

device:
  name: "pi-pond-cam"
  stream_port: 8080
  # Host to bind to (0.0.0.0 for all interfaces)
  host: "0.0.0.0"

camera:
  # Resolution: 640x480 (recommended for Pi Zero), 1280x720, 1920x1080
  resolution: "640x480"
  # Framerate: 10 (recommended for Pi Zero), 15, 20, 30
  framerate: 10
  # JPEG quality: 1-100 (lower = faster, 70 recommended for Pi Zero)
  jpeg_quality: 70
  # Camera rotation: 0, 90, 180, 270
  rotation: 180
  # Horizontal flip
  hflip: false
  # Vertical flip
  vflip: true
  # Auto white balance
  awb_mode: "auto"
  # Exposure mode
  exposure_mode: "auto"
  # Metering mode
  meter_mode: "average"

power:
  # Auto-sleep after inactivity (seconds)
  auto_sleep_timeout: 600
  # Night standby mode
  night_standby:
    enabled: true
    # Start hour (24h format)
    start_hour: 22
    # End hour (24h format)
    end_hour: 7

# Logging
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  log_file: "/var/log/signalk_pi_pond_video.log"
